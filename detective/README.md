## Running the detective package<br>
### The package has external dependencies like torch etc. for which a venv named YOLO has been created, before running any of the executables, source the detector_setup.bash file in the root of your workspace which craetes and  activates the venv and copies the respective trained models to the package shared directory
