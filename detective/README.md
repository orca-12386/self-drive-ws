## Running the detective package

### The package has external dependencies like torch etc. for which a venv named YOLO has been created, before running any of the executables, source the env.bash file in the package directory which activats the venv and copies the respective trained models to the package shared directory
