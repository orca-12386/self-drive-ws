## Running the detective package<br>
### The package has external dependencies like torch etc. for which a venv needs to be created,after you build the package,source the detector_setup.bash file in the root of your workspace which creates and  activates the venv and copies the respective trained models to the package shared directory
